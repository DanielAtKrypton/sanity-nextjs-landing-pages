language: node_js
dist: xenial

node_js:
    - lts/*

before_install:
- npm install
- npm run update
- npm run build

after_install:
# - npm run dev

deploy:
# deploy master to production
  - skip_cleanup: true 
  - provider: script
   script: bash scripts/deploy.sh production $GH_DEPLOY_KEY
   on:
     branch: master